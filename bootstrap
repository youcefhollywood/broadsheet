#!/bin/bash
# FC custom-runtime entry. Dependencies are pip-installed into /code via the WebIDE
# terminal (pip install -t .), so they import directly. Runs the Flask app on the
# listening port using Python's built-in WSGI server (no gunicorn needed).
export FC_SERVER_PORT="${FC_SERVER_PORT:-9000}"
cd /code
exec python3 app.py
